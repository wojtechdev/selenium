{
  "dependencies": {
    "chai": "^6.2.2",
    "mocha": "^11.7.5",
    "mochawesome": "^7.1.4",
    "selenium-webdriver": "^4.39.0"
  },
  "scripts": {
    "test": "mocha --require ts-node/register tests/**/*.ts --no-timeouts --reporter mochawesome --reporter-options reportDir=reports,reportFilename=report",
    "test:parallel": "mocha --require ts-node/register tests/**/*.ts --no-timeouts --parallel --reporter mochawesome --reporter-options reportDir=reports/json,reportFilename=[name].json",
    "build": "tsc",
    "test:watch": "mocha --require ts-node/register tests/**/*.ts --no-timeouts --watch --watch-files tests/**/*.ts,utils/**/*.ts"
  },
  "devDependencies": {
    "@types/chai": "^5.2.3",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.0.3",
    "@types/selenium-webdriver": "^4.35.4",
    "mochawesome-merge": "^5.1.0",
    "mochawesome-report-generator": "^6.3.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}

